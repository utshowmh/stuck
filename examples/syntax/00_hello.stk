# comments starts with a `#` and ends with `newline`. there's no multiline comment.

"hello, world" print   # prints operand to stdio (it's like print(420))

# every program have to end with a `newline`