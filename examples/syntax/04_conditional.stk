# Working with Conditional Control Flow

var 100 @x
var 50 @y

if x y < then      # `then` takes the operand and if it's 0 (true) then jumps to next `else` or `end` otherwise executes until found any `else` or `end`
    69 dump
else x y > then
    420 dump
else
    96 dump
end                 # `end` ends the `if` block