# Working with Loops

var 10 @a

while a 0 > do      # `then` takes the operand and if it's 0 (true) then jumps to next `else` or `end` otherwise executes until found any `else` or `end`
    a dump
    a 1 - @a
end                 # `end` ends `while` block