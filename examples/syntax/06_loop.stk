# Working with Loops

10 @i

while i 0 > do      # `then` takes the operand and if it's 0 (true) then jumps to next `else` or `end` otherwise executes until found any `else` or `end`
    i writeln
    i 1 - @i
end                 # `end` ends `while` block
