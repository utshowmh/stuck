# A Counter Program

0 @count                # counter variable
100 @limit              # limit to count to
    
while count limit < do
    count 1 + @count        # increaments counter variable
    count writeln           # prints counter variable
end 
