# A program that calculates Factorial.

10 @limit                           # The nth number (calculate until).
1 @fatctorial                       # Calculated factorial.

while limit 0 > do
    fatctorial limit * @fatctorial  # Calculating factorial (using n * n - 1 * ... * 1).
    limit 1 - @limit
end

fatctorial writeln